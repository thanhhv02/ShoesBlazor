
<style>
    .number {
        background-color: red;
        display: inline-block;
        border-radius: 50%;
        width: 50px;
        height: 50px;
        text-align: center;
    }
</style>

<li class="nav-item dropdown">

    <a class="nav-link nav-icon" href="#" data-bs-toggle="dropdown">
        <i class="bi bi-bell"></i>
        <span class="badge bg-primary badge-number">@notifications.Count(p => !p.IsRead)</span>
    </a><!-- End Notification Icon -->

    <ul style="max-height:60vh; max-width: 400px; width: 80vw; overflow:hidden; overflow-y:auto;" class="dropdown-menu dropdown-menu-end dropdown-menu-arrow notifications row col-12">
        <li class="dropdown-header">
            <a @onclick="ReadAllNoti" href="Javascript:void(0)"><span class="badge rounded-pill bg-primary p-2 ms-2">Đánh dấu tất cả là đã đọc đã đọc</span></a>
        </li>
        <li>
            <hr class="dropdown-divider">
        </li>

      

        @foreach (var item in notifications)
        {
            var isread = item.IsRead ? "bi bi-check-circle text-success" : "bi bi-info-circle text-primary";
            <li style="cursor:pointer;" @onclick="() => ReadNoti(item)" class="notification-item">
                <a href="@item.DataUrl" class="notification-item">
                    <i class="@isread"></i>
                    <div>
                        <h4>@item.Title</h4>
                        <p>@item.Message</p>
                        <p>@AppExtensions.TimeAgo(item.Created)</p>
                    </div>
                </a>
            </li>

            <li>
                <hr class="dropdown-divider">
            </li>
        }

    </ul><!-- End Notification Dropdown Items -->

</li><!-- End Notification Nav -->




   
    
